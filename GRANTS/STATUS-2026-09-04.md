# FactLedger — Status & Next Steps (2026-09-04)

This file records only verified status. It must not claim a submission, award,
production check, or external response unless there is evidence for it.

## Product status

- Core code, tests, CI, and security checks are implemented.
- Render service deployment is live at the configured production service URL.
- Independent API health verification is still required before claiming the
  health endpoint is externally verified.
- Dashboard production deployment remains blocked until `VERCEL_TOKEN` exists
  in GitHub Actions Secrets.
- Evidence/Risk Graph core is merged; its current storage adapter is in-memory
  and is not a durable production historical database.
- Live Solana mainnet/WebSocket behaviour remains unverified against a dedicated
  production RPC.
- ChainGPT deterministic fallback is implemented; a real-key live request still
  requires external verification.

## Funding status

- Awarded to date: **$0**.
- Solana Foundation and Colosseum application materials exist in the repository.
- Do **not** mark a grant, accelerator, bounty, or funding application as
  submitted until the official portal confirms submission.
- Do **not** state an expected reply date unless the official programme provides
  one for the specific application.
- Any KYC/KYB, legal certification, SAFE/equity/token terms, tax declaration, or
  binding acceptance must be completed by the project owner in the official
  portal.

## Priority order

1. Add `VERCEL_TOKEN` privately in GitHub Actions Secrets; never commit or paste
   the token into issues or chat.
2. Complete Dashboard deployment and verify Dashboard → API authentication/CORS.
3. Verify the production API health endpoint from an external client.
4. Move Solana RPC/WebSocket verification to a dedicated provider account.
5. Verify a real ChainGPT request while preserving deterministic fallback.
6. Complete the public demo and record a short walkthrough.
7. Submit funding/accelerator applications only after the official forms are
   reviewed and owner-only identity/legal fields are completed.
8. Recruit design partners and record real usage before claiming traction.

## Security reminders

- Public receiving addresses may be shared when required by an official funding
  programme; private keys, seed phrases, recovery material, API keys, and access
  tokens must never be committed.
- FactLedger remains read-only: no signing path and no custody of user funds.
