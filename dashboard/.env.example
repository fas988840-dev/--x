# URL of the FactLedger REST API (src/ at the repo root) this dashboard reads from.
FACTLEDGER_API_URL=http://localhost:3000

# Only needed if that API has API_KEYS set. Server-side only - never
# exposed to the browser (do NOT prefix with NEXT_PUBLIC_).
# FACTLEDGER_API_KEY=your-key-here
